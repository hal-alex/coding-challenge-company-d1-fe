import { Box, Typography } from "@mui/material"
import React from "react"

const CreateElevator = () => {
  return (
    <Box>
      <Typography>Can't find the right elevator? Create one here</Typography>
    </Box>
  )
}

export default CreateElevator
